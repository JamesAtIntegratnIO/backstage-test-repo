apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: demo-5-pvc
  labels:
    app: demo-5
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-client
  volumeName: demo-5-pv